/**
 * CustomCursor — disabled.
 *
 * The site previously rendered a custom gold ring + dot following the mouse.
 * At the owner's request, the native system arrow is used sitewide; the
 * gold sparkle-on-text effect (see GoldSprinkle) remains the signature.
 *
 * This component is kept as a no-op so App.js imports continue to work
 * without requiring a structural refactor.
 */
export default function CustomCursor() {
  return null;
}
